# AgroStat API – Environment Variables
# Copy this file to .env and adjust values as needed.

# Server
HOST=0.0.0.0
PORT=8000
DEBUG=false

# CORS – comma-separated list of allowed origins
# Use * to allow all origins (not recommended for production)
CORS_ORIGINS=*
